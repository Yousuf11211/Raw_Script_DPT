# Install with: pip install -r requirements.txt
# Optional GPU packages are commented -- install them only if you need GPU support.

pandas>=2.0,<3.0
numpy>=1.24,<3.0
matplotlib>=3.8,<4.0
seaborn>=0.13,<1.0
scikit-learn>=1.3,<2.0
joblib>=1.3,<2.0
imbalanced-learn>=0.12,<1.0
xgboost>=2.0,<3.0
# Optional GPU detection (install only if you need it and a compatible CUDA toolchain)
torch>=2.0,<3.0
tensorflow>=2.12,<3.0

